BLOCK

Second-pass, pre-implementation review. Read the working brief, issue #46, the first Codex review, Revision 2 and the now-existing #11 dashboard plan, then inspected the relevant source. `46:N` references `2026-09-08-tables-dates-settings.md`; `11:N` references `/Users/sean/dev/AgentScope-wt/11/docs/superpowers/plans/2026-09-08-dashboard.md`. Code references are relative to this worktree unless stated otherwise. “Closed” means the plan resolves the finding, not that an implementation has passed tests. No tests/builds, git writes or implementation edits were performed; this review is the only file written.

## Original P1 closure

1. **[P1] Application-wide number locale — OPEN, substantially improved.** `46:86–109,370–422,728–744,847–853,1019–1034` now inventory direct formatters, migrate KPI/coverage/chart/receipt/import/assistant consumers, and test an ambient consumer under the production provider composition. Source inspection confirms why the `App` subscription works: `App.tsx:39–56` recreates route elements, while `shellContext.tsx:25–38` passes children through. The original direct-call omissions at `bars.tsx:50`, `primitives.tsx:150–166`, `charts.tsx:17,23,45,68`, `Sessions.tsx:22` and `Imports.tsx:12` are explicitly covered.

   Closure is incomplete: the claimed complete inventory still misses unformatted presentation numbers, for example `bars.tsx:51` (`imports`), `Session.tsx:70` (coverage), `:72,79,81` (diagnostic/call counts), and `Import.tsx:128` (file count). The Session file entry does not assign these conversions. A ban on `toLocaleString` cannot catch raw JSX/interpolated numbers. The revision also claims mounted Imports-count coverage without actually naming that assertion or the requested import/assistant locale test in `46:838–862`. Finally, `46:1004–1007` permits dropping assistant/import number migration, directly contradicting `46:1218–1223` and the issue's every-page requirement. **Required:** extend the semantic inventory and file assignments, add large-count mounted Imports and assistant/import assertions, delete that cut option, and resolve the exact-text integration finding C4 below.

2. **[P1] Storage precedence/cross-tab synchronization — CLOSED at plan level.** `46:298–340,788–811,1036–1049` distinguish successful persistence from failed writes: a persisted local choice no longer wins forever after an external event; an unpersistable choice remains local. The policy covers removal, `clear()`, malformed values, storage failures, theme synchronization and reset. Both-tabs-write tests and fresh module initialization replace the inadequate cached reread. This addresses the actual `chosen` precedence in `theme.ts:7–14,24–34` while retaining the stale-storage/throwing-write regression at `components.test.tsx:123–139` and the pre-paint bootstrap at `index.html:9–14` / `main.tsx:12`. These are concrete implementation and validation instructions, not an assertion that the unchanged theme code already synchronizes tabs.

3. **[P1] #11 agreement and Model-cell ownership — OPEN overall; Model-cell ownership CLOSED.** `46:214,612–615,741,1059–1064` now explicitly convert the actual `Session.tsx:36` Model cell, target `/sessions`, and leave it text until the key is supported. Current `scope.ts:12–14` and `routers.py:199–208` confirm only Source/Agent are supported today; `sessionsTable.tsx:12–22` has no Model column. `11:537–539` agrees #46 owns interactive Source/Agent/Model cells. However, declaring #46's interface authoritative did not establish agreement. The sibling plan exists and has the concrete conflicts below; the absence claims at `46:183–197,1051–1053` must be replaced with a dated joint contract.

## Contract findings and exact changes needed in both plans

The plans agree on the four **base** keys in order (`source`, `agent`, `model`, `period`), Source/Agent/Model labels and identity formatting, `7d → last 7 days`, absent period meaning unrestricted, #46 owning cell links, and #11 owning the period filter/facets/API integration. Keep those decisions. Model is not a new Sessions-table column requirement: #46 converts the existing call-table cell; any future column uses its component.

- **C1 [P1] Drill preservation and clearing disagree.** `46:199–222` says charts use `set()` and chips enumerate only `SCOPE_KEYS`; `46:588–606` preserves every other parameter on same-route edits but drops non-base parameters across routes. `11:278–316` instead introduces URL `drill`, `setDrill/removeDrill`, an extra chip, cross-route preservation, and removal of the envelope on Period changes. Consequently a Session Model link can lose a tool/accounting/day drill, while a Period chip removal can preserve stale date witnesses. The current `scopeSearch` in both worktrees (`scope.ts:32–36`) only serializes base keys, so preservation requires actual integration work.

  **Change #46:** adopt the validated URL envelope as part of the shared scope; distinguish base-key order from the full serialized scope; carry `drill` through target-route links; make `patchScope` apply the agreed drill reconciliation for every entry point, including cell links, chip removal and Clear all. Replace “charts call set” with #11's drill helper for server-returned scopes. **Change #11:** explicitly route base edits through that same `patchScope` contract; specify deletion as well as overrides. Its current “start from envelope then overlay base values” (`11:295–298`) would retain `scope.model` after removing `model` from the URL. Delete the cleared dimension and its mutually exclusive unknown predicate from the effective envelope, or explicitly invalidate that drill; never leave a removed filter silently active. Resolve `11:564–565` (“changing a base filter removes stale witness state”) against `11:312–315` (Source/Agent/Model preserve it). Both plans need tests for a tool/accounting drill through a Session cell target, Model removal, Period removal, Clear all and Back, asserting effective API scope and displayed chips together.

- **C2 [P2] The chip contract is only partially shared.** `46:212,632–643,835–836` requires new visible-tooltip attributes and focus handling; `11:261–266,299,320,503–507,651–653` freezes markup/tooltips while adding a derived chip. `46:211` also allows optional chart placement, while its focus fallback assumes a table caption.

  **Change #46:** publish a typed `ScopeChips` props contract, including `keys?: readonly ScopeKey[]`, `label?: string`, whether derived chips are included under a key subset, and how the focus target is supplied/resolved. Fix one placement: above the Overview sessions panel and Sessions table, with base chips followed by the optional derived chip. Include the derived chip in empty/Clear-all/focus logic. **Change #11:** adopt that exact contract and placement; replace “markup/tooltip unchanged” with “public leaf props unchanged; preserve #46's `.has-tip`/`data-tip` upgrade.” It owns the envelope extension, not a second chip renderer. Both plans should name the same post-removal keyboard test.

- **C3 [P2] Period and merge-order details remain contradictory.** `46:206` says the new keys arrive when endpoints accept them, but `11:269–276` correctly defines Period as UI-only, resolved into UTC date bounds. `46:223–225` promises no code changes if #11 lands first even though `46:207` says #46 ships only Source/Agent labels; #11 adds label/formatter entries only “when those helpers are present” (`11:256–259`). `46:228–229` says #11 does not touch `bars.tsx`, whereas `11:503–504` replaces its filter controls and receipt.

  **Change #46:** copy the complete period contract (`all` omitted, `7d/30d/90d`, UTC midnight bounds through today, `Period (UTC)` control); activate Period only once client date-bound resolution and the receiving endpoints work, never send literal `period`. State that a #11-first integration must initialize all four labels and the period formatter. Correct the bars/shared-file ownership account. **Change #11:** state the reciprocal bootstrap responsibilities and preserve the agreed `ScopeChips`/tooltip changes in either merge order. Both plans must choose one coordinator-owned integration order/checkpoint instead of claiming both orders need no adaptation.

- **C4 [P1] The number contract breaks when #11 replaces numbers with authoritative text.** #46's helpers accept numbers or leave strings unchanged (`46:380–383,409–422`); #11's exact KPI, coverage, receipt and chart values become text (`11:328–371,452–455,503–511`). Mechanical `num(number)` substitutions do not survive that rewrite. Rendering transport text unchanged also cannot satisfy locale grouping/decimal preferences; converting it through `Number` would violate #11's lossless requirement.

  **Change #46:** add a shared exact-decimal-text presentation formatter in `format.ts`, with locale/settings inputs, preserving the original text separately for copying and transport; no floating-point conversion of authoritative values. **Change #11:** require this formatter for visible exact lines, partitions, receipts, tooltips, focus hints and accessible tables, retaining raw text for API/copy and approximate numbers only for geometry. Both plans must test a locale change on the merged dashboard with a value above `Number.MAX_SAFE_INTEGER` and a fractional decimal. Assign these integration edits explicitly; “#11 touches no number formatting” (`46:216`) is not executable.

## Additional findings

- **[P2] 29 hours is an effort estimate, not a release-safe critical path.** The rows at `46:980–987` correctly sum to 29 hours, but only three hours cover documentation, #11 integration, full CI and review fixes. `46:999–1000` schedules first combined acceptance and accessibility evidence on September 11, the day #19 needs for release. The consolidated plan explicitly freezes features before Day 4 (`docs/planning/2026-09-07-consolidated-plan.md:118–122`). #11 itself estimates 18–26 hours plus 2–4 contingency (`11:675–681`). #39's supplied brief covers the assistant field editor, numeric codec and report entry point, overlapping #46's `assist/DocumentEditor.tsx`, assistant surfaces and `pages/Imports.tsx`; #46 never schedules that handoff. Parallel effort does not eliminate these integrations. **Required:** retain 29 hours as a provisional lower-bound working budget, add explicit #11/#39 integration and review contingency, and target an integrated, green acceptance candidate by `2026-09-10T23:00Z`, leaving September 11 to #19. Name owner availability and checkpoint dependencies; at eight hours/day, 29 hours already exceeds September 8–10's 24 hours before contingency. If that cannot fit, use the cuts below or renegotiate scope/deadline explicitly.

- **[P2] Accepted timestamp precision exceeds the arithmetic precision.** `46:426–428,761` accepts up to nine fractional digits, while `46:507–510,531–539` represents instants and exact spans only to microseconds. A reversed pair ending `.123456789Z` / `.123456788Z` cannot be distinguished by integer microseconds. **Required:** constrain acceptance to the API's zero-to-six fractional digits and reject higher precision consistently in parser/tests, or use nanosecond arithmetic and up to nine exact fractional digits. The six-digit API-only contract is the smaller scope.

## Scope recommendation — five lines

1. Ship all browser display choices, defaults/reset, persistence/private-mode fallback, the accessible `/settings` page and retained theme shortcut, with same-tab live updates on every page.
2. Ship every ordinary timestamp conversion, viewer/UTC/named-zone formats, relative on/off, Session offsets, exact ISO reveal/copy, human durations and honest Unavailable; keep source/editor payloads verbatim.
3. Ship Source/Agent anchors, active-value clearing, shared removable chips and Back; convert the existing Model cell now and activate it with #11; keep period controls and server drill semantics owned by #11.
4. Ship complete locale presentation including assistant/import counts and #11's lossless text, focused unit/propagation tests, required Playwright/Axe and screenshots, with integrated acceptance green by September 10 for #19's last day.
5. Cut exhaustive Gallery expansion, the proactive formatter lint ban and automatic minute-by-minute relative aging to follow-ups; cross-tab synchronization and its extra browser matrix can also be deferred if same-tab live-update/reload evidence replaces them. Do not cut mandatory timestamps, locale consumers, accessibility or contract integration.

REVIEW-DONE
