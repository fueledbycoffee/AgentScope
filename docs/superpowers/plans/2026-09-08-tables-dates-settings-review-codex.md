BLOCK

Reviewed the working brief, issue #46, and implementation plan in the requested order, then checked the cited implementation, components, tests, design records, and supporting backend/date contracts. Review checkout: `feat/46-tables-dates-settings`, HEAD `8828f5a`; the plan describes its earlier inspection at `8aab14f`. References `Plan:N` below mean `docs/superpowers/plans/2026-09-08-tables-dates-settings.md:N`.

This is a pre-implementation review. Evidence consists of source inspection and read-only Node reproductions of the proposed date primitives. No implementation, dependency installation, build, browser/Axe sweep, or git write was performed. The requested sibling plan at `/Users/sean/dev/AgentScope-wt/11/docs/superpowers/plans/2026-09-08-dashboard.md` was absent on both checks. Consequently, agreement with #11 is unverified; no disagreement with an unread sibling document is asserted.

1. **[P1] Number locale will remain inconsistent across the application.** **Location:** Plan:250–254, 537–561, 704–709.

   **What is wrong / evidence:** Adding defaults to `display` and `abbreviate` does not intercept direct `toLocaleString('en-US')` calls. Examples explicitly outside the proposed edits are `components/bars.tsx:50` (scope receipt), `components/primitives.tsx:150,158–166` (KPI exact values and coverage), `components/charts.tsx:17,23,45,68` (tooltips, accessible table, focus hints), and the import/assistant components. Even edited pages retain hard-coded formatting at `pages/Sessions.tsx:22` and `pages/Imports.tsx:12`. Changing the locale to German could produce an abbreviated German KPI beside an English exact value and English coverage. Re-rendering cannot repair a hard-coded locale. This directly misses the acceptance requirement that grouping and decimals follow the browser preference on every page.

   **Specific change:** Inventory and migrate all presentation numbers, including receipts, counts, tooltips, accessibility text, and already formatted strings. Keep ids, raw JSON, and lossless source strings untouched. Expand the exhaustive file list and coordinate edits to bars/primitives/charts with #11. Add mounted-page locale tests for a KPI with exact value and coverage, the scope receipt, Imports counts, and an import/assistant surface, using values large enough to reveal grouping.

2. **[P1] The private-mode precedence rule defeats the promised cross-tab synchronization.** **Location:** Plan:192–218, 591–605; `web/src/theme.ts:7–14,24–34`.

   **What is wrong / evidence:** The proposed `chosen` value wins over storage after any local write, while the proposed storage-event handler only invalidates the parsed cache. Reproduction by following those rules: tab A chooses `dmy`; tab B writes `mdy`; A receives the event and rereads, but its `chosen=dmy` still wins. A test receiving an event before any local write would miss this. The retained theme module has no storage listener at all, so another tab's theme change or reset cannot update this tab's toggle or root attribute. A fresh read/remount also does not prove disk persistence while the module's fallback is still populated.

   **Specific change:** Define one coherent snapshot/event policy: validated external updates must replace the effective snapshot and reconcile the fallback; specify behavior for key removal, `storage.key === null`, malformed external values, and storage failures. Preserve the tested case where reads return stale storage but writes throw. Extend theme synchronization without replacing its pre-paint bootstrap. Test two-way updates after both tabs have made local choices, reset propagation, and persistence after a fresh module/page initialization rather than only a cached reread.

3. **[P1] The #11 dependency is a proposed contract, not an agreed implementation boundary; model cells have no complete owner.** **Location:** Plan:122–153, 546–561, 689–693; issue #46 Coordination.

   **What is wrong / evidence:** The sibling file is absent, so the issue's condition to run after #11 or agree first has not been met. The plan assumes `period=7d`, generic string formatting, and #11 adding records that #46 creates; that ownership is not executable as described if #11 lands first. It also says #46 owns table cells while #11 owns its Model column. The existing Model cell is actually `pages/Session.tsx:36`; `sessionsTable.tsx:12–22` currently has no Model column. The planned Session edit never converts that existing cell. Extending `SCOPE_KEYS` alone cannot turn a `display(call.model)` into a link.

   **Specific change:** Record a matching contract in both plans before shared implementation, or sequence #46 after the reviewed #11 result. Agree on all four keys (`source`, `agent`, `model`, `period`), period values/default/clear semantics, labels and formatting, helper return types, the exact `ScopeChips` props and placement, and owners for every Model cell. Explicitly cover the Session call-table model link with a `/sessions` target once the API supports it. Account for both merge orders and shared test/style files; do not describe integration as merely a two-line extension. This is a missing agreement, not evidence of a sibling-plan disagreement.

4. **[P2] The primary Playwright date acceptance path cannot pass with the planned defaults.** **Location:** Plan:172, 306–333, 635–657; `web/e2e/smoke.spec.ts:65–143`.

   **What is wrong / evidence:** Smoke creates import attempts during the test run. The new Imports Started cell prefers relative text and `relativeTimes` defaults to true. Choosing `dmy` therefore leaves these rows saying `just now` or `N min ago`, not matching the absolute-date regex. Navigating from Settings to Imports also mounts a new Imports page; it does not prove that an already visible page updates. The proposed optional second tab only checks shared persistence, not an update received by an already mounted ledger. The fixed radio example is zone-dependent, but the test does not pin a time zone.

   **Specific change:** Use stable control labels, pin the browser clock and zone, and turn relative times off for the absolute-format scenario (test the relative toggle separately). Keep Imports open in one page and change Settings in another page of the same browser context; assert that the original ledger changes without a reload, navigation, or remount, then reload to prove persistence. Retain a separate same-tab navigation path. Locate the ISO comparison by the exact session id and timestamp field, rather than an arbitrary timestamp versus the first API row.

5. **[P2] The advertised propagation test does not protect the ambient number-formatting path.** **Location:** Plan:234–241, 635–636, 704–709; `web/src/App.tsx:39–56`, `web/src/shellContext.tsx:25–38`.

   **What is wrong / evidence:** The proposed `App.useSettings()` subscription is a valid solution to the current children pass-through: App recreates the routed elements, whereas a ShellProvider-only update would not. However, the only named propagation test changes a `DateText`, which subscribes directly. It can pass even if the App subscription is omitted, leaving ambient number readers stale. It therefore does not pin the claimed propagation proof or guard a future memo boundary. Existing `App.test.tsx:30–31` also mounts without the provider, so that path alone will not exercise production context wiring.

   **Specific change:** Test a mounted ordinary number consumer that has no direct settings subscription, under the real SettingsProvider/App/ShellProvider composition. Assert changed number output and preserved local page state/DOM identity. Cover the providerless fallback separately. Use explicit subscriptions/settings props at memoized boundaries rather than claiming one directly subscribed date test protects all pages.

6. **[P2] UTC offsets are incorrectly specified, and session call timestamps silently lose the required offset.** **Location:** Plan:303–305, 324–333, 549, 575; `web/src/pages/Session.tsx:37–38,48–49,65–66`.

   **What is wrong / evidence:** The proposed `timeZoneName: 'longOffset'` plus `GMT` → `UTC` replacement yields `UTC` at zero offset, not the promised/tested `UTC+00:00`. This was reproduced locally with `Intl.DateTimeFormat('en-US', { timeZone: 'UTC', timeZoneName: 'longOffset' }).formatToParts(...)`. Only Interval facts receive `offset: true`; the issue says session pages show absolute time with the offset. During the Paris fall-back, `2026-10-25T00:30:00Z` and `2026-10-25T01:30:00Z` both render `02:30`, but have offsets `+02:00` and `+01:00` respectively. Without offsets, different call instants become visually indistinguishable. Existing proposed zone tests cover neither DST transition nor a fractional-hour offset.

   **Specific change:** Normalize zero offset explicitly and compute the zone offset at the input instant. Apply the offset requirement to all session timestamps, including model/tool tables. Name tests for spring-forward and fall-back in a DST zone, UTC, a viewer zone pinned independently of the host, and `Asia/Kathmandu` (`+05:45`), plus midnight/noon and all three formats. Specify Gregorian calendar, hour cycle and digit policy when assembling parts; `formatToParts` alone does not guarantee engine-independent localized month names.

7. **[P2] `Date.parse` plus a finite guard accepts fabricated dates and ambiguous inputs.** **Location:** Plan:264–268, 284–291, 568–579.

   **What is wrong / evidence:** Read-only Node reproductions: `Date.parse('2026-02-30T12:00:00Z')` is finite and becomes `2026-03-02T12:00:00.000Z`; `'0'` is also accepted; an offset-free `'2026-09-08T14:05:00'` is interpreted in the machine's zone. These values pass the proposed guard and can receive a misleading exact-ISO label. Testing only an impossible month (`2026-13-45`) does not expose calendar normalization. Stored backend datetimes are UTC-aware (`backend/src/agentscope_app/infrastructure/db/models.py:41–55`), so accepting arbitrary JavaScript date syntax is unnecessary. Invalid zone/locale/options must also not escape as formatter exceptions; an invalid injected `now` has no defined behavior.

   **Specific change:** Specify and validate the accepted timestamp grammar and calendar components before formatting (at least the API's zoned ISO format), retaining the original valid string separately. Reject impossible calendar dates and ambiguous offset-free input as `Unavailable`; never infer an instant from them. Add leap-day, invalid day, offset-free, whitespace/numeric-string, malformed settings, and nonfinite `now` cases, checking both non-throwing behavior and all fields of the unavailable result.

8. **[P2] Span/duration precision and invalid-number handling do not meet the stated exactness contract.** **Location:** Plan:270–277, 335–356, 580–585, 617–618.

   **What is wrong / evidence:** The plan preserves API microseconds for date copying but specifies only three fractional digits for “exact” span seconds. Using the cited `Date.parse` primitive, subtracting end `2026-09-08T14:05:00.123455Z` from start `2026-09-08T14:05:00.123456Z` produces `0` ms, hiding a reversed interval as a real zero. Even a positive sub-millisecond interval loses its exact duration. The duration rejection rule names null/NaN/negative but omits undefined and positive infinity despite the signature. Display rounding at unit boundaries is unspecified. Missing either endpoint is assigned `no start timestamp` in the design although the named test promises a distinct missing-end reason. Finally `DurationText({ms})` cannot itself know a reversed span, contrary to its named component test.

   **Specific change:** Parse/compare interval endpoints at the precision the API supplies, and retain enough precision for exact seconds; use rounded values only for the human label. Reject all nonfinite/nullish inputs, distinguish missing start/end from invalid start/end, and define rounding/carry behavior at 1 s, 10 s, 60 s, 60 min and 24 h. Test sub-millisecond positive and reversed spans, equal instants, infinities, zero, and invalid endpoints. Put reversal/reason assertions on `SpanText`. Clarify how the report lead exposes exact seconds when replacing its current string-only `seconds()` call at `pages/Imports.tsx:67`.

9. **[P2] Relative-time rounding and boundary behavior are contradictory or untested.** **Location:** Plan:306–320, 576–577, 611–612.

   **What is wrong / evidence:** `N` has no rounding definition. At 45 seconds, floor produces `0 min ago`; near 90 minutes, rounding can produce `90 min ago` immediately before switching to hours. The table labels a future instant under 45 seconds `just now`, but the following prose says future instants are never clamped to `just now`. Tests at 30 s, 2 min, 89 min, 3 h, 35 h, 6 d and 8 d are examples inside ranges, not tests of their boundaries. A `3 h ago` label does not normally become `4 h ago` after one minute unless the starting instant is deliberately near a specified rounding boundary.

   **Specific change:** Choose a rounding/minimum-count policy, resolve the near-future wording, and define “under 7 days” as elapsed duration or calendar days. Test just below, exactly at, and just above 45 s, 90 min, 36 h and 7 d in both directions, plus relative-off and a controlled minute-tick crossing. Verify the interval starts/stops across mount/unmount and StrictMode without leaving timers or test settings behind.

10. **[P2] Persisted preferences can be valid but unrepresentable in the form; invalid-zone warnings are unreachable as written.** **Location:** Plan:188–210, 475–485, 596–598.

    **What is wrong / evidence:** The reader replaces an unknown zone with the default, discarding the evidence needed for the Settings warning. Conversely, valid zones need not appear in `supportedValuesOf`: local reproduction accepts `US/Eastern` and resolves it to `America/New_York`, but `supportedValuesOf('timeZone')` does not include `US/Eastern`. The select only appends the stored value in the fallback-list path, so normal engines can display a different selection from the effective stored zone. The store accepts supported BCP-47 locales such as `fr-CA`, but the six-item locale select cannot represent them either.

    **Specific change:** Canonicalize validated zones/locales and always include the current supported choice in options, or constrain validation to the documented supported choices. Carry validation diagnostics separately if an unknown-value warning is promised. Add tests for a valid zone alias, an unsupported stored zone, a valid locale outside the preset list, unavailable `supportedValuesOf`, and reset keeping every displayed control consistent with effective defaults.

11. **[P2] Scope-link mutation semantics and regression tests are incomplete.** **Location:** Plan:145, 405–445, 619–627; `web/src/scope.ts:16–70`.

    **What is wrong / evidence:** `scopeHref` is named but its implementation contract never specifies query encoding, preservation of other scope keys/non-scope parameters, whitespace normalization, history behavior, or equivalence with `toggle`. Existing `set`, `scopeKey`, and `scopeSearch` use `URLSearchParams`; those guarantees do not automatically transfer to a new link builder. The new tests only use `codex`, and no actual new-tab or keyboard activation test is named. Clearing the agent in a URL containing an encoded model and period must not discard either. The active-value and target-route naming rules also need precedence: an active source must still announce clearing even when the target is `/sessions`.

    **Specific change:** Define one pure URL patch/toggle operation shared by all three helpers, using `URLSearchParams` and the same normalized equality rules as `readScope/set`. Specify offset reset, preservation of other scope dimensions, same-route versus target-route treatment of unrelated query/hash, and browser Back. Test `trace & lab`, a model containing `+ & = # /` and Unicode, four-key removal/clear-all, offset reset on both add and clear, target-route active clearing, and Enter plus middle/modifier-click opening the already-correct href without changing the original tab. Preserve existing session-id navigation anchors (`sessionsTable.tsx:13`); “ids stay text” must not remove that route.

12. **[P2] Reusing the controls unchanged does not satisfy the promised keyboard/tooltips contract.** **Location:** Plan:147, 394–403, 443–445, 486–492; `web/src/components/bars.tsx:45–46`, `web/src/AppShell.tsx:25–26`, `web/src/styles/base.css:36–41,75–77,174–176`.

    **What is wrong / evidence:** The chip remove button has a name and native `title`, but no `.has-tip`/`data-tip` visible keyboard tooltip. Promoting it from Gallery to production unchanged misses the shell's established icon-control contract. The rail is a `role="group"` of `aria-pressed` buttons, not the `radiogroup` described by the plan; simply changing the container role leaves missing radio children and keyboard semantics. Its `.theme-toggle { overflow: hidden }` also clips the below-control pseudo-tooltip and can clip the outward focus outline. Duration/unavailable-reason spans and the proposed dense-table `copy={false}` fallback expose information only to hover, not keyboard focus. Removing a focused chip unmounts the control without a specified next focus target.

    **Specific change:** Preserve ScopeChip's props but upgrade its implementation to the visible tooltip pattern and add it to the touched files. Use native labelled theme radios in a fieldset, or deliberately keep the named pressed-button group; implement the complete semantics of whichever is chosen. Fix tooltip/focus clipping. Provide keyboard access to duration reasons/exact values and retain an accessible copy/reveal path if reducing timestamp tab stops. Name browser tests for Tab/Enter, focus after chip removal, selected theme state, visible focus rings and tooltips in both themes, and the final table row/scroll edge. A role-name query or Axe alone cannot prove these visual interactions.

13. **[P2] The Axe baseline policy can hide new violations and is not yet reproducible.** **Location:** Plan:555–556, 660–663, 716–721; `web/package.json:26–39`, `web/playwright.config.ts:19–34`.

    **What is wrong / evidence:** There is no Axe dependency or checked-in baseline. Disabling a whole rule because an old instance fails also exempts new Settings/chip/date controls from that rule, so it cannot establish “no new violations.” The five-route sweep does not explicitly require loaded data, an active chip, or test the edited report and already-imported notice. The dependency is appropriate as a devDependency, but the lockfile is `web/pnpm-lock.yaml`, not the root `pnpm-lock.yaml` listed by the plan. CSP was checked: `web/index.html`, Vite config, Playwright config, and `backend/src/agentscope_app/interfaces/api/main.py` contain no current CSP enforcement; there is no evidence for an existing CSP blocker or a need to weaken production security.

    **Specific change:** Pin the Axe dependency through the web manifest/lockfile and run a baseline against the pre-change revision with the same engine, route states, and themes. Fix violations or compare narrowly identified existing rule/target instances, allowing no new targets; do not globally disable rules. Wait for real table rows and deliberately scan scoped states plus the changed report/import surfaces. Record successful injection and a completed analysis; if CSP later interferes, diagnose it and isolate any test-only accommodation rather than changing application policy. Keep manual keyboard/focus checks alongside Axe.

14. **[P2] The acceptance inventory overstates coverage and offers unauthorized narrowing.** **Location:** Plan:114–117, 551, 568–683, 725–727; `web/e2e/assist.spec.ts:75–76`, `web/src/assist/ActionBar.tsx:70–74`.

    **What is wrong / evidence:** The claim that no existing E2E test asserts a rendered timestamp is false: the assistant test asserts `/2025-09-04T15:33:2\d/` in its emission JSON preview. Leaving that raw-data preview intact is sensible, but the plan must explicitly distinguish source/preview JSON from display timestamps instead of claiming every on-screen timestamp is reformatted. Dropping `Import.tsx:264` would leave an ordinary `imported_at` receipt raw and directly fail “replace every raw timestamp rendering”; that is not equivalent coverage. The `_at` search cannot certify this invariant by itself: valid `<DateText value={row.started_at}>` usages still match, while serialized timestamps can have no literal `_at` at the rendering site. The existing App mock returns one session regardless of scope (`App.test.tsx:24–26`), so new tests need scope-sensitive fixtures to prove actual narrowing, not just an outgoing query. Playwright displays at most 50 unscoped rows (`format.ts:3`, `Sessions.tsx:14`), not all 80 fixture sessions, and the assistant project later adds two sessions to the shared database.

    **Specific change:** Keep the already-imported receipt conversion mandatory; explicitly exempt verbatim payload/mapping/emission inspection, retaining or deliberately updating the assistant assertion. Maintain a semantic timestamp inventory and manually inspect search results. Specify scope-sensitive unit fixtures, 40 data rows versus 41 including the header, restoration of the paginated list and receipt, and deterministic project ordering or source-scoped expectations independent of assistant mutations. Add the missing named source-link test, and reset settings/module state between tests so non-default preferences do not contaminate existing assertions.

15. **[P2] “About one day” conceals the integration critical path before September 11.** **Location:** Plan:732–740; `docs/planning/2026-09-07-consolidated-plan.md:140`.

    **What is wrong / evidence:** The listed tasks sum to **10.5 hours**, already more than a conventional eight-hour day. They omit the required direct-number migration, actual #11 agreement/integration, DST/precision and cross-tab fixes, an initial Axe baseline with unknown findings, and the expanded keyboard/browser validation. Parallel #11 work shares scope, tables, Overview, components, and likely E2E configuration/test fixtures. Separate edits to array entries do not remove those semantic dependencies. The sprint ends 2026-09-11; no dated integration checkpoint or review/retest buffer is reserved.

    **Specific change:** Re-estimate the complete file/test scope and publish a dated critical path: agree the four-key/chip contract on September 8, implement and validate independent settings/date work September 9–10, and reserve September 11 for combined #11/#46 acceptance, accessibility evidence and review fixes. Budget at least the actual 10.5 hours plus explicit migration/integration/baseline contingency, and state the dependency if #11 is late. Do not recover schedule by omitting mandatory timestamps, locale consumers, or new-violation detection.

16. **[P3] A few evidence/documentation claims should be corrected before the plan becomes the implementation checklist.** **Location:** Plan:53–61, 351–356, 558; `docs/architecture/import-pipeline.md:3–6`; `research/insights/MERGED.md:443–445`.

    **What is wrong / evidence:** The 212 inversions cited by the plan are inside source `timing_events`; the research explicitly says bounds come from min/max. This is evidence to test defensive reversal handling, not proof of 212 reversed normalized session intervals. Adding Settings to an architecture document that explicitly describes a frozen historical commit also makes its stated snapshot inaccurate unless the document's scope changes. Several “current state” line references have drifted (for example wall latency is Session line 50, not 52).

    **Specific change:** Correct the inversion claim and current line references. Put the new route in the ADR amendment/current documentation, or deliberately update the architecture snapshot's date/revision and surrounding claims rather than making an isolated historical-diagram edit.

What the plan gets right:

- The goal reproduces the issue's expected result, and the store/context, formatter, component, route, test and screenshot surfaces are concrete enough to review before code exists.
- URL scope remains the sole filtering mechanism. Real cell anchors, active-value clearing, per-key removable chips, and preserving source/agent while extending model/period are the right direction. Routing an Imports source to Sessions is justified: `listImports` has no scope parameter and the Console design explicitly exempts the ledger. Its provenance “Source” buttons are not source-dimension cells and should remain provenance actions.
- Recognizing ShellProvider's children pass-through is correct. Having App consume settings can propagate changes through the current non-memoized routed tree; directly subscribed date components also update in stored shell-bar nodes. The missing part is complete consumer migration and a test that actually exercises that ambient path.
- Cached external-store snapshots, per-field validation, a storage-failure fallback, no server preference API, defaults/reset, and preserving the theme bootstrap are appropriate architectural choices once the cross-tab rules are reconciled.
- Preserving the API ISO string verbatim is essential. Absolute tables, relative ledger dates, viewer/UTC/named-zone choices, explicit unavailable values, missing/reversed-span defenses, and visible observed-span caveats fit the product. The plan correctly avoids interpreting observed span or tool latency as active work.
- A Console `/settings` page with labelled native controls, live samples, an accessible rail entry, reset confirmation and an ADR amendment fits the requested scope. Reusing existing tokens and table primitives avoids an unnecessary redesign.
- The proposed unit/integration/browser layers, fixture-backed filtering round trip, clipboard failure handling, screenshots and full verification commands are useful. Identifying the missing Axe dependency and baseline up front is honest; the baseline procedure needs the stricter regression policy above.

REVIEW-DONE
