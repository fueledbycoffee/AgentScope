Review of issue #9 plan against `main` at `ee2e21bd83a527cd7d0bd350c1fa7ca3ba2a567f`.
Code citations below refer to that commit; concurrent working-tree implementation edits were excluded. Plan citations refer to `docs/superpowers/plans/2026-09-07-import-accounting.md` as reviewed (95 lines).

1. **P1 — Native-key overlap is not ADR-002's suspected duplicate.** Plan:39–50 labels every cross-file native-key match, including equal projections, as suspected. `docs/adr/ADR-002-identities.md:48–58` requires matching scoped claims with *different canonical entity projections*; native IDs alone establish neither duplication nor conflict.
   Define claim scope, equal/different projection handling, null keys and composite mapping keys; compare child projections independently of parent changes. The actual mapping uses `trace_key`, not `round_id` (`backend/mappings/tracelab-v1.json:30–33,73`), and emissions support tuples (`backend/src/agentscope_app/domain/mapping/interpreter.py:315–320`). Preserve every accepted occurrence and metric total.

2. **P1 — The proposed warning transport corrupts accounting categories and loses attribution.** Plan:41–46 puts an emission count into entity counts and each file's record outcomes. `TraceRepository.store` returns only aggregate inserted-entity counts (`backend/src/agentscope_app/application/ports.py:116–129`); these are copied straight into `report.entities` (`backend/src/agentscope_app/application/use_cases/imports.py:304–320`). There is no per-file return channel.
   Introduce a separate typed diagnostics result keyed by file and locator; populate `RecordOutcome.warning_counts` and an additive file warning field. Keep exactly five record outcomes: `_counts` currently discards other keys (`imports.py:400–401`), while bypassing it makes emission warnings inflate record totals. Preserve existing `absent`/`null` diagnostics (`docs/adr/ADR-003-nulls-and-units.md:22–40`; `web/src/api/types.ts:16–21,47`).

3. **P2 — The advertised indexed query cannot run against this schema.** Plan:50–51 assumes an `(source, external_id)` index on calls. Neither call table has an `external_id` column; claims are JSON `native_key`, indexed only indirectly by source/occurrence and session (`backend/src/agentscope_app/infrastructure/db/models.py:203–257`).
   Specify an indexed claim representation and migration/backfill, or explicitly budget scans. With 500-row chunks (`backend/src/agentscope_app/infrastructure/db/repositories.py:516–520,770–774`), the fixture alone needs 10 model plus 12 tool lookups; unindexed claim predicates can rescan all observations for that source per chunk. Bound bind parameters and returned matches, and measure query plans on a populated database, including repeated claims.

4. **P2 — Cross-file detection is dependent on chunk and file order unless defined explicitly.** Plan:43–50 tests sequential A then B only; the store receives emissions from every pending file together (`backend/src/agentscope_app/application/use_cases/imports.py:266–289,304–309`). Query-before-insert misses another file in the same chunk, but can see it in an earlier chunk (`backend/src/agentscope_app/infrastructure/db/repositories.py:516–520`).
   Decide whether peers in the current attempt participate and whether only later emissions or both files receive warnings. Count each qualifying incoming emission once, regardless of how many prior matches exist. Test same-chunk/cross-chunk overlaps, reversed file order, repeated keys within one file, and overlaps with previously committed files.

5. **P2 — The duplicate outcome browser has no rows to browse.** Plan:24–29,61–63 offers all outcomes from report counts, but replay attempts persist no results (`backend/src/agentscope_app/application/use_cases/imports.py:252–257,376–379`); mixed batches create results only for pending files (`imports.py:266–287`). Selecting “duplicate (4,770)” will return nothing.
   Define virtual replay outcomes from original locators or explicitly disclose that replay details are unavailable, including historical attempts. Do not rerun mappings, copy original accepted outcomes as this attempt's accepted rows, or change the already-correct duplicate counters. Test pure replay, mixed batches and failed attempts.

6. **P2 — Numeric ordering and pagination need an executable contract.** Plan:25–27,61–70 does not specify the SQL order or page transitions. Locators are strings in the primary key (`backend/src/agentscope_app/infrastructure/db/models.py:158–165`); lexical order gives `line:1, line:10, line:2`. Sorting a fetched page cannot fix global ordering.
   Apply combined filters, then SQL ordering by file hash and integer suffix with a deterministic tie-breaker, then limit/offset. Test `line:2/10/100`, zero-based `row:0/2/10`, multiple files and page boundaries; assess the cast sort's cost. Reset offsets on every filter/import change and retain Previous/Next: `DataTable` has no pagination (`web/src/components/primitives.tsx:39–44,116–127`). The fixture's 4,770 rows require paging, not one response (`docs/api/v0.1.md:7–8`).

7. **P2 — Summary facets do not define filtered totals or their counting grain.** Plan:30–35 supplies three independent global maps without filter arguments. These cannot determine the count for `code AND rule_id AND file_sha256`, nor whether counts beside options should change with other filters. One partial record can produce several rejects (`backend/src/agentscope_app/application/use_cases/imports.py:76–83,94–106,320`).
   Specify that summary values count reject rows, not rejected source records; define global versus filtered facets and empty maps/404 behavior. Add a filtered total if the UI needs it, or label global counts and use page-length navigation. Test multiple rejects per locator, intersections and empty filtered results. Document additive endpoints/DTOs in `docs/api/v0.1.md:96–102`; preserve the existing rejects array and positional call compatibility (`backend/src/agentscope_app/application/use_cases/queries.py:77–89`; `backend/src/agentscope_app/interfaces/api/routers.py:99–108`).

8. **P2 — The promised original-import link has no report field.** Plan:55–60 requires a duplicate to point at its original import, but `ImportReport`/`FileInfo` carry no such reference (`backend/src/agentscope_app/application/dto.py:126–150`). The commit path reduces `find_committed` results to a boolean (`backend/src/agentscope_app/application/use_cases/imports.py:372–373`).
   Add source-scoped original references per duplicate file, including read support for historical reports. A batch can have multiple originals and mapping revisions: the top-level mapping intentionally echoes only the first file (`docs/api/v0.1.md:83–86`). Render mixed mappings honestly in the file bar/history rather than presenting that revision as universal; test direct report reloads.

9. **P2 — The history proposal removes outcomes that are currently visible.** Plan:69 limits record counts to accepted/rejected/duplicate; current history displays all five (`web/src/pages/Imports.tsx:21`). An all-partial or all-ignored committed import can consequently look empty, and reject-row counts must not replace rejected-record counts.
   Retain partial/ignored visibility and define the status sentence's denominator across mixed batches. Keep status-based entity dashes (`research/design/claude/3-console/README.md:106–108`) distinct from known zero entity counts; preserve the existing fixture totals and exact-file replay assertions (`backend/tests/interfaces/test_api_e2e.py:73–76,116–118`).

10. **P2 — Payload highlighting will usually do nothing, and some drawers cannot load.** Plan:63–66 promises a raw drawer with the offending path highlighted. Reject `path` is an emission path such as `tool_call[2]`, not a JSON field path (`backend/src/agentscope_app/application/use_cases/imports.py:94–104`); `JsonText` only highlights the first literal substring (`web/src/components/primitives.tsx:98–102`).
    Resolve the offending field/value to a span in exact `payload_text`, or disclose unavailable highlighting; matching arbitrary text is not structural highlighting. Reuse `SourceRecordDialog` to preserve large integers (`web/src/components/source.tsx:9–30`; `docs/api/v0.1.md:129–131`). Define the unavailable-payload state for invalid JSON: null stored payloads currently produce 404 (`backend/src/agentscope_app/application/use_cases/queries.py:122–126`). Test nested arrays, repeated values, absent fields and integers above 2^53.

11. **P2 — The proposed tests cannot establish collision safety or accounting invariants.** Plan:78–85 relies on the fixture and a half-overlap example. Direct inspection of the committed gzip finds 4,770 distinct `round_id` and 4,770 distinct `trace_key` values; upstream collisions are explicitly not fixture measurements (`docs/adr/ADR-002-identities.md:14–19`; `fixtures/tracelab/tracelab-sample.manifest.json:4–18`).
    Add synthetic equal/different projections, missing/composite claims and independent session/source scopes. Assert record/file/attempt warning reconciliation without altering accepted, partial, duplicate, entity or token totals. Extend rollback/race checks to the new diagnostics and readers; the existing injected-failure baseline is `backend/tests/infrastructure/test_multifile_import.py:244–258`. A summary-shape assertion alone will miss incorrect counts and filter behavior.

12. **P3 — The one-day estimate omits unresolved implementation work.** Plan:89–93 allocates 0.2 day to duplicate detection on a nonexistent index and 0.4 day to UI whose provenance/filter contracts are incomplete. Budget claim indexing/migration, projection semantics, per-record diagnostics, replay references, typed API/client changes and adversarial tests before committing to that estimate.
    My planning estimate is 2–3 days for the stated scope after decisions above; retain one day only by explicitly deferring cross-file diagnostics and reducing the UI scope. Existing drawers/tables reduce rendering work, but do not implement these missing contracts (`web/src/components/source.tsx:13–30`; `web/src/components/primitives.tsx:116–127`).

BLOCK
